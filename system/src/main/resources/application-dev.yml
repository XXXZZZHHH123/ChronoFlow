spring:
  datasource:
    url: jdbc:mysql://${PORD_DB_HOST}:${PORD_DB_PORT}/${PORD_DB_NAME}?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8
    username: ${PORD_DB_USER}
    password: ${PORD_DB_PASSWORD}
  data:
    redis:
      host: ${PORD_REDIS_HOST}
      port: ${PORD_REDIS_PORT}
      database: ${PORD_REDIS_DATABASE}
      password: ${PORD_REDIS_PASSWORD}

# log config
logging:
  level:
    nus.edu.u: debug
    org.springframework: warn
    com.zaxxer.hikari: debug

chronoflow:
  security:
    mock-enable: true
  captcha:
    enable: false
  cookie:
    http-only: false
    security: false
